import { FileSearchPipe } from './file-search.pipe';

describe('FileSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FileSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
